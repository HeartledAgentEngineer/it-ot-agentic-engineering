# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
*.egg
.venv/
venv/
env/

# Environment
.env
*.env.local
*.env.production
*.env.bak

# ChromaDB
chroma_data/

# Auftragsbuch fuer den Coding-Agenten: enthaelt, was diktiert wurde,
# und dessen Ergebnisse. Aus demselben Grund draussen wie chroma_data.
auftraege.json
auftraege.json.tmp

# Gesichter-Katalog (Personen-Merkliste): private Personendaten.
# Aus demselben Grund draussen wie chroma_data / auftraege.json.
gesichter_katalog.json
gesichter_katalog.json.tmp

# Persoenlicher Teil des System-Prompts: Profil, Geraete, Haltung.
# Das Repo ist oeffentlich - das hier geht niemanden etwas an, der sich
# den Code ansieht. Vorlage: backend/system_prompt.local.example.md
system_prompt.local.md

# Hochgeladene Nutzer-Dateien (private Bilder/PDFs) und Backup-Ablagen.
uploads/
.verlauf_backup_*/
backups/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Node
node_modules/
npm-debug.log*

# Docker
docker-compose.yml
# Lokale Face-ONNX-Modelle (aus OpenCV Zoo, gross; werden lokal gehalten)
ml_models/
