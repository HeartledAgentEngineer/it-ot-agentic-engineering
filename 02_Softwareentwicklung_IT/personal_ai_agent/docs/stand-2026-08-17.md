# Stand 17.08.2026 — Übergabe

Fortschreibung von `stand-2026-08-14.md`. Was seit dem 14.08. entstand, was
heute repariert wurde, was offen ist.

---

## Was heute gemacht wurde

### Auftragskette repariert (Commit `88ac7df`)

Drei Fehler liessen die Kette Agent → Auftragsbuch → Hermes still abreissen.
Alle drei am Geraet nachgewiesen, nicht vermutet:

1. **Kurz-ID lief ins Leere.** Sichtbar ist ueberall nur die achtstellige ID,
   Hermes und der Watcher melden damit zurueck — die Schreiboperationen
   verglichen aber exakt. Jede Rueckmeldung endete mit 404, den
   `curl -s >/dev/null` verschluckte. Am laufenden Handy-Backend belegt:
   Kurz-ID 404, volle ID 200.
2. **`am start` mit ungueltigem Component-Namen.** Hermes ist nie von allein
   gestartet. Richtig ist `com.hermesagent.android/com.hermes.android.MainActivity`
   — Paket und Klasse liegen in verschiedenen Namensraeumen.
3. **Zeichensatz zerstoerte Meldungen.** `sys.stdin.read()` dekodierte mit der
   Umgebungseinstellung; `⚠️` warf beim Server HTTP 500.

Nebenbei gefunden: zonenloser Zeitstempel warf `TypeError` (haette bei
3-Sekunden-Takt die Kette dauerhaft blockiert), und ohne brauchbares `python3`
verschluckte der Watcher Auftraege stumm.

### Erster Prüfbefehl des Projekts

```bash
cd backend && python -m pytest tests -q
```

21 Tests in `backend/tests/test_auftrag_service.py`. Gegen den alten Code
fallen 10 davon um — die Suite faengt also, was sie soll.

### Harness-Analyse

`docs/agent-harness-analyse.md` — inklusive zweier Nachtraege mit dem
gescheiterten Installationsversuch. Kurzfassung: Weg A („fertigen
Kommandozeilen-Agenten uebernehmen") ist am Geraet gescheitert.

---

## Offene Punkte — nach Dringlichkeit

### 1. Fix ist auf GitHub, aber noch nicht auf dem Handy

`88ac7df` ist gepusht. **Solange das Handy nicht gezogen hat, laeuft dort
weiter der kaputte Watcher.** Auf dem Handy:
```bash
cd ~/it-ot-agentic-engineering && git pull origin main \
  && ./termux-hermes-watcher.sh stop && ./termux-hermes-watcher.sh
```

### 2. Der Watcher läuft nicht

Am Geraet geprueft: `pgrep -af hermes-watcher` → kein Prozess. Deshalb liegen
zwei Auftraege seit 17.08. 15:00 unberuehrt im Buch (`a2411f71`, `023a5fd8`,
beide „Test").

### 3. Entscheidung Agent-Harness

Weg A ist tot (siehe Analyse). Es bleiben zwei Moeglichkeiten:
* **Codex-Portierung** (`@mmmbuto/codex-cli-termux`) — laeuft, aber fremder
  Nachbau mit Shell-Zugriff neben dem API-Schluessel, ausgetauschte TLS-Pruefung.
* **Eigenes schlankes Harness im Backend** — Python 3.13 laeuft, OpenRouter,
  Auftragsbuch und Pruefbefehl stehen bereits.

### 4. Slice 1.3/1.4 aus dem Plan — nicht gebaut

* Watcher-Lebenszeichen (`.hermes-watcher.alive`) in `/api/health` auswerten
* „seit X Minuten" im Auftrags-Panel
* `frontend/app.js:2197` und `frontend/hermes_status.html:47` nehmen
  `relevant[relevant.length-1]` als „neuester" — die Liste ist absteigend
  sortiert, das ist der **aelteste**. Auf `relevant[0]` korrigieren.

### 5. Sicherheit — unbearbeitet

* `POST /api/auftraege` hat **keine Auth** (`auftraege.py:32-35`), Server bindet
  `0.0.0.0`, Klartext-HTTP. Jedes Geraet im WLAN kann Auftraege schreiben,
  Ergebnisse faelschen und den Agenten befragen. Auth-Infrastruktur ist
  vorhanden und ungenutzt (`router/auth.py`, JWT).

### 6. Log-Spam am Handy-Backend

Zwei bis drei `WARNING: Invalid HTTP request received` **pro Poll-Zyklus**,
dauerhaft. Ursache unbekannt. In diesem Rauschen geht jede echte Fehlermeldung
unter — genau darauf war heute mehrfach Verlass noetig.

### 7. Doku — Slice 3 aus dem Plan, nicht angefasst

* `CLAUDE.md`: sechs sicherheitsrelevante Aussagen stimmen nicht mehr
  (kein externer STT/TTS, nur Text an OpenRouter, Embeddings lokal, nur
  localhost). Ein Agent, der sich daran haelt, entscheidet falsch.
* `README.md`: ~30 % falsch — Projektstruktur, 6 von 34 Endpunkten, ChromaDB
  (real ein JSON-Store), Startweg.
* `docs/deployment-termux.md`: beschreibt F-Droid, installiert ist die
  **Google-Play-Fassung** (`googleplay.2026.06.21`).
* `docs/hermes-integration-2026-08-14.md:192-197`: Repo-Pfad ist
  `~/it-ot-agentic-engineering`, nicht `~/agent_project/...`.
* `.gitignore:31` verweist auf `backend/system_prompt.local.example.md` — gibt
  es nicht.
* Toter Router `backend/app/router/test_upload.py`, nirgends registriert.

### 8. Nicht weiterverfolgt: Bild-Erkennung

Der urspruengliche Anlass („Modell liest den Zettel nicht") wurde heute nicht
bearbeitet. Vermutung aus der Erkundung, **ungeprueft**: `max_tokens: 4096`
deckelt bei gpt-5-nano Denk- und Ausgabe-Tokens gemeinsam
(`llm_service.py:419`), das Modell verbraucht das Budget im Reasoning und
liefert leeren Inhalt ohne Fehlermeldung. Dazu `detail: "auto"` statt `"high"`
und kein OCR-Prompt.

---

## Geräte-Eckdaten (gemessen 17.08.2026)

Motorola edge 50, Android 16, arm64-v8a, Snapdragon 8 Gen 1, 8 Kerne,
11,5 GB RAM, 292 GB frei. Termux googleplay.2026.06.21.

Vorhanden: Python 3.13.13, Node v26.3.1, npm, git 2.54.0, rustc, cargo,
clang, make, curl, tmux, uv 0.11.24, binutils.
Fehlt: bun, deno, go, jq.

Repo auf dem Handy: `~/it-ot-agentic-engineering`
